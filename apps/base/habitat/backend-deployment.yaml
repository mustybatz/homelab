apiVersion: apps/v1
kind: Deployment
metadata:
  name: habitat-backend
spec:
  selector:
    matchLabels:
      app: habitat-backend
  template:
    metadata:
      labels:
        app: habitat-backend
    spec:
      imagePullSecrets:
        - name: habitat-backend-pull
      containers:
      - name: habitat-backend
        image: mustybatz/habitat-backend:dev-43bf5b29-1749671221 # {"$imagepolicy": "habitat-dev:habitat-backend-latest"}
        resources:
          requests:
            memory: "100Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 4000
